import 'package:flutter/material.dart';

class SystemSimulator extends StatelessWidget {
  const SystemSimulator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Placeholder(),
    );
  }
}
